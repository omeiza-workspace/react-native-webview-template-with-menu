import Home from '@/components/Home';
import React from 'react';

/**
 * WebViewScreen 2026
 * Registration is now handled globally in RootLayout via usePushNotifications.
 * This component simply renders the Home (WebView) view.
 */
export default function WebViewScreen() {
  return <Home />;
}
